name: child_test_simple

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest

    env:
      WORKFLOW_ID: wf_test_simple_001
      SHEET_ROW: "2"
      EXPIRES: "300"
      LOCK_SECONDS: "650"

    steps:
      - name: Preflight GET healthcheck
        shell: bash
        env:
          GATEWAY_URL: ${{ secrets.GATEWAY_URL }}
        run: |
          set -euo pipefail
          RESP="$(curl -sS -L "$GATEWAY_URL" || true)"
          echo "$RESP" | head -c 200 || true
          echo
          echo "$RESP" | jq -e '.service=="eou_gateway_webapp"' >/dev/null

      - name: START (GET signed payload)
        id: start
        shell: bash
        env:
          GATEWAY_URL: ${{ secrets.GATEWAY_URL }}
          GATEWAY_SECRET: ${{ secrets.GATEWAY_SECRET }}
        run: |
          set -euo pipefail

          NOW="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          BODY="$(jq -nc \
            --arg workflow_id "$WORKFLOW_ID" \
            --argjson row "$SHEET_ROW" \
            --arg phase "start" \
            --argjson expires "$EXPIRES" \
            --argjson lock_seconds "$LOCK_SECONDS" \
            --arg now "$NOW" \
            '{workflow_id:$workflow_id,row:$row,phase:$phase,expires:$expires,lock_seconds:$lock_seconds,now:$now}')"

          SIG="$(printf "%s" "$BODY" | openssl dgst -sha256 -hmac "$GATEWAY_SECRET" -hex | awk '{print $2}')"

          # base64url encode BODY (no padding)
          P="$(printf "%s" "$BODY" | openssl base64 -A | tr '+/' '-_' | tr -d '=')"

          URL="${GATEWAY_URL}?p=${P}&sig=${SIG}"

          RESP="$(curl -sS -L -H "Accept: application/json" "$URL" || true)"
          echo "RESP first 200:"
          echo "$RESP" | head -c 200 || true
          echo

          echo "$RESP" | jq -e . >/dev/null
          LOCKED="$(echo "$RESP" | jq -r '.locked // false')"
          echo "locked=$LOCKED" >> "$GITHUB_OUTPUT"

      - name: Exit fast if locked
        if: steps.start.outputs.locked == 'true'
        run: |
          echo "Locked already. Exiting."
          exit 0

      - name: DO WORK (dummy)
        run: |
          sleep 3

      - name: FINISH (GET signed payload)
        if: always() && steps.start.outputs.locked != 'true'
        shell: bash
        env:
          GATEWAY_URL: ${{ secrets.GATEWAY_URL }}
          GATEWAY_SECRET: ${{ secrets.GATEWAY_SECRET }}
        run: |
          set -euo pipefail

          STATUS="${{ job.status }}"
          case "$STATUS" in
            success) CONCLUSION="success" ;;
            failure) CONCLUSION="failure" ;;
            cancelled) CONCLUSION="cancelled" ;;
            *) CONCLUSION="neutral" ;;
          esac

          NOW="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          BODY="$(jq -nc \
            --arg workflow_id "$WORKFLOW_ID" \
            --argjson row "$SHEET_ROW" \
            --arg phase "finish" \
            --arg conclusion "$CONCLUSION" \
            --argjson expires "$EXPIRES" \
            --argjson lock_seconds "$LOCK_SECONDS" \
            --arg now "$NOW" \
            '{workflow_id:$workflow_id,row:$row,phase:$phase,conclusion:$conclusion,expires:$expires,lock_seconds:$lock_seconds,now:$now}')"

          SIG="$(printf "%s" "$BODY" | openssl dgst -sha256 -hmac "$GATEWAY_SECRET" -hex | awk '{print $2}')"
          P="$(printf "%s" "$BODY" | openssl base64 -A | tr '+/' '-_' | tr -d '=')"
          URL="${GATEWAY_URL}?p=${P}&sig=${SIG}"

          RESP="$(curl -sS -L -H "Accept: application/json" "$URL" || true)"
          echo "RESP first 200:"
          echo "$RESP" | head -c 200 || true
          echo

          echo "$RESP" | jq -e . >/dev/null
          echo "Finish OK: $(echo "$RESP" | jq -c .)"
